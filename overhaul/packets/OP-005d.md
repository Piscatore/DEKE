# OP-005d: Distill intent — standalone briefs + ad-hoc notes

## Status: pending

## Contract

- **Goal:** Append to `docs/INTENT.md` one section per design intention found
  in the remaining standalone `thoughts/` artifacts that don't belong to the
  Federation, Knowledge Base Foundation, or Advisory Pipeline threads — the
  last OP-005 sub-packet.
- **Context budget:**
  - ALWAYS: `docs/GLOSSARY.md`, `overhaul/STATE.md`
  - THIS PACKET: `overhaul/CHARTER.md` (escalation path);
    `OVERHAUL-SKETCH-v0.2.md` §3.3 (INTENT.md template), §4 (packet contract
    format); `thoughts/shared/briefs/docs-overhaul-brief.md`,
    `thoughts/shared/briefs/product-checkpoint-fixes-brief.md`,
    `thoughts/shared/work/llm-provider-config-brief.md`,
    `thoughts/adhoqnotes.md` (4 files, ~314 lines total — `adhoqnotes.md` is
    a single-line note: "Investigate and add vectorless rag and a very
    intelligent hybrid model that uses the best from both worlds" — an
    unbuilt forward-looking idea, not a past decision; treat its INTENT.md
    section as entirely `(inferred, unconfirmed)` since there is no
    surrounding context to distill from); `docs/INTENT.md` (exists after
    OP-005a/b/c — append, do not overwrite existing sections).
  - Known ties to prior packets: `llm-provider-config-brief.md` likely bears
    on **ADR-0006/ADR-0007** (Llm/ Gemini-OpenAI backend) — read for
    relevant background, not itself re-escalated unless a genuinely new
    angle surfaces. `docs-overhaul-brief.md` may bear on the
    top-level-docs-never-adopted-the-package-model `PARKING-LOT.md` item
    (OP-003) — cross-check, don't re-log if it's the same finding.
- **Tool budget:** Read/Grep/Glob (required). No Edit/Write outside
  `docs/INTENT.md` and this packet's own Status header.
- **Recommended tier:** Sonnet-class, default thinking (per
  `docs/TOOLING.md` model/thinking tiers table's mapping-sweep tier).
- **Inputs:** OP-001..004f (`docs/PROJECT-MAP.md`/`PARKING-LOT.md`
  cross-check); OP-005a/b/c (`docs/INTENT.md` exists, append-only).
- **Outputs:** `docs/INTENT.md` (appended — this is OP-005's final
  sub-packet, so `docs/INTENT.md` is considered complete after this);
  `overhaul/PARKING-LOT.md` (appended if plan/built drift or other minor
  issues surface); `overhaul/STATE.md` (new top entry, naming OP-006 as
  next since the whole OP-005 series completes here); this packet file's
  Status header.
- **Done when:** `docs/INTENT.md` has at least one new section per design
  intention found in the 4 source files, each ≤ half a page, each with
  `What Mikael wants` / `Why (origin/context)` / `Constraints it imposes` /
  `Source`; anything synthesized rather than directly stated is flagged
  `(inferred, unconfirmed)`; `STATE.md` has a new top entry naming OP-006
  (interview packet) as next.
- **Human touchpoints:** none directly — flagged intentions (including all
  of `adhoqnotes.md`'s, per above) surface for confirmation at OP-006, not
  resolved here.
- **Out of scope:** Federation, Knowledge Base Foundation, Advisory Pipeline
  MVP (each its own completed OP-005 sub-packet); fixing any plan-vs-built
  drift inline; editing `docs/PROJECT-MAP.md` or `docs/GLOSSARY.md`
  (read-only cross-check only); overwriting or restructuring OP-005a/b/c's
  `docs/INTENT.md` sections; starting OP-006 itself (that's the next
  packet, not this one).

## Escalation path (standard, applies to every packet)

If this packet uncovers a design or feature problem:

1. Do **not** fix it inline.
2. Write a `proposed` ADR (type: design) with the finding and options.
3. Note it in `STATE.md` and in this packet's output.
4. The adjudication packet (OP-006/OP-007) routes it to Mikael; if accepted,
   the ADR's `Consequences` field defines the new packet(s).

## Handover

On completion, append a `STATE.md` entry: section count, any ADR escalated,
any parking-lot items logged, how many sections were flagged
inferred/unconfirmed, that the OP-005 intent-distillation series is now
fully complete, and that OP-006 (interview packet — closed questions from
OP-003..005, including all escalated design ADRs) is next.
