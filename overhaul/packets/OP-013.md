# OP-013: Dissolution — archive /overhaul, tag repo, close subproject

## Status: done

## Contract

- **Goal:** Close the Overhaul subproject per `OVERHAUL-SKETCH-v0.2.md` §6
  (packet line) and §8 (after-exit note): archive `/overhaul` in place,
  tag the repo, and complete the doc pass OP-012's handover deferred here
  (mark HYG-2 done in `docs/ROADMAP.md`).
- **Context budget:**
  - ALWAYS: `overhaul/STATE.md`
  - THIS PACKET: `OVERHAUL-SKETCH-v0.2.md` §6+§8; `CHARTER.md`;
    `packets/OP-012.md` (handover); `overhaul/PARKING-LOT.md` (disposition
    check); `docs/ROADMAP.md` (HYG-2 rows).
- **Tool budget:** Read/Grep/Glob; git CLI; doc-maintainer for the
  `docs/` edit; `AskUserQuestion` (front-loaded decisions).
- **Recommended tier:** default; no code changes, no design decisions.
- **Inputs:** OP-012 done — all 8 exit criteria PASS; nothing blocks
  dissolution.
- **Outputs:** this packet file; archive banners in `STATE.md` +
  `CHARTER.md`; final `STATE.md` entry; `docs/ROADMAP.md` HYG-2 done-mark
  + dissolution milestone row; git tag; final commit pushed.
- **Done when:** `/overhaul` carries an unambiguous ARCHIVED marker with
  no live "read this every session" directive; ROADMAP reflects HYG-2
  complete and the dissolution milestone; the dissolution commit is
  tagged and pushed.
- **Human touchpoints (front-loaded):** archive mechanics, tag name,
  push-or-local — all three asked and answered before execution.
- **Out of scope:** any `src/` change; any new design work; executing
  ROADMAP packets (that is post-Overhaul work); deleting or moving any
  `/overhaul` file.

## Decisions (Mikael, 2026-07-14, via AskUserQuestion)

1. **Archive mode: in place + banner.** `/overhaul` stays at its path —
   the ~15 historical provenance references from `docs/PROJECT-MAP.md`,
   `docs/INTENT.md`, `docs/ROADMAP.md`, `docs/GLOSSARY.md`, `docs/INDEX.md`
   keep resolving. Rejected: move to `/archive/overhaul` (breaks those
   references or forces a wide doc edit for zero benefit).
2. **Tag name: `overhaul-complete`** (annotated tag on the dissolution
   commit). Rejected: `overhaul-dissolved`, `v0.2-overhaul`.
3. **Push: yes** — dissolution commit + tag pushed to
   `origin` (`github.com/Piscatore/DEKE`) after local verify.

## Execution record (2026-07-14)

- **Pre-flight:** `PARKING-LOT.md` verified fully dispositioned — all 15
  entries carry a Promoted/Resolved/Reviewed(stays-parked) marker; nothing
  undecided rides into dissolution. No pre-existing git tags; working tree
  clean at start.
- **Archive banners:** `STATE.md` header directive replaced with an
  ARCHIVED banner (explicitly: do not load `/overhaul` into agent context
  budgets; live docs are `/docs`). `CHARTER.md` status line flipped from
  TEMPORARY SUBPROJECT to ARCHIVED, per its own "archived, not deleted"
  clause.
- **ROADMAP doc pass (via doc-maintainer, per Documentation Governance):**
  see below.
- **Tag + push:** see below.

## Handover (post-Overhaul)

- `/overhaul` is history. Agents: orient from `docs/INDEX.md`; plan work
  from `docs/ROADMAP.md`'s packet DAG (NOW lane after this packet: P1-1
  first priority, RES-1, HYG-1, HYG-3 — HYG-2 done by OP-012's acceptance
  test).
- Per §8's after-exit note, DEKE's own `/docs` are now the first knowledge
  base DEKE should learn to serve about itself —
  `BootstrapIngestionService` (`--bootstrap`) already implements that
  ingestion; running it is regular roadmap work, not Overhaul work.
