# OP-005a: Distill intent — Federation (phase 1 + phase 2)

## Status: pending

## Contract

- **Goal:** Produce `docs/INTENT.md` (new file) with one section per major
  Federation-related design intention, distilled from the phase 1 and phase
  2 `thoughts/` artifacts.
- **Context budget:**
  - ALWAYS: `docs/GLOSSARY.md`, `overhaul/STATE.md`
  - THIS PACKET: `overhaul/CHARTER.md` (escalation path);
    `OVERHAUL-SKETCH-v0.2.md` §3.3 (INTENT.md template), §4 (packet contract
    format); `thoughts/shared/research/federation-phase1-research.md`,
    `thoughts/shared/plans/federation-phase1-plan.md`,
    `thoughts/shared/sessions/federation-phase1-session.md`,
    `thoughts/shared/briefs/federation-phase2-search-brief.md`,
    `thoughts/shared/research/federation-phase2-search-research.md`,
    `thoughts/shared/plans/federation-phase2-search-plan.md`,
    `thoughts/shared/sessions/federation-phase2-search-session.md` (7 files,
    ~1334 lines total); `docs/PROJECT-MAP.md`'s Federation, Federation
    (Infrastructure), Federation Endpoints, Search Endpoints, Search Tools,
    and Peer Health Check entries (cross-check only — what was actually
    built, to separate confirmed intent from drifted-and-abandoned plan).
  - Known ties to prior packets: `docs/architecture/federation.md`'s ranking
    formula under-documentation is already escalated as **ADR-0005**
    (proposed) — relevant background, not re-escalated here. Plan-vs-built
    drift on non-ranking details (e.g. a planned feature that shipped
    differently) is expected and normal — log to `PARKING-LOT.md`, do not
    treat as a design smell unless it contradicts `CHARTER.md`'s non-goals.
- **Tool budget:** Read/Grep/Glob (required). No Edit/Write outside
  `docs/INTENT.md` and this packet's own Status header.
- **Recommended tier:** Sonnet-class, default thinking (interpretive
  synthesis of already-decided plans, not open design work — per
  `docs/TOOLING.md` model/thinking tiers table's mapping-sweep tier).
- **Inputs:** OP-001..004f (`docs/PROJECT-MAP.md`'s Federation-related
  entries, for cross-check).
- **Outputs:** `docs/INTENT.md` (new file, created by this packet, one
  section per Federation-related intention per the §3.3 template);
  `overhaul/PARKING-LOT.md` (appended if plan/built drift or other minor
  issues surface); `overhaul/STATE.md` (new top entry); this packet file's
  Status header.
- **Done when:** `docs/INTENT.md` exists with at least one section per major
  Federation-related design intention found across the 7 source files, each
  ≤ half a page, each with `What Mikael wants` / `Why (origin/context)` /
  `Constraints it imposes` / `Source` (citing the actual file(s), not just
  "inferred"); anything synthesized rather than directly stated in a source
  file is explicitly flagged `(inferred, unconfirmed)` for OP-006's
  interview rather than presented as fact; `STATE.md` has a new top entry
  naming OP-005b as next.
- **Human touchpoints:** none directly — distillation is additive, not a
  decision. Flagged `(inferred, unconfirmed)` intentions surface for
  confirmation at OP-006, not resolved here.
- **Out of scope:** Knowledge Base Foundation, Advisory Pipeline MVP, or the
  docs-overhaul/product-checkpoint-fixes/llm-provider-config/adhoq-notes
  threads (each its own OP-005 sub-packet — OP-005b/c/d); fixing any
  plan-vs-built drift inline; editing `docs/PROJECT-MAP.md` (read-only
  cross-check only).

## Escalation path (standard, applies to every packet)

If this packet uncovers a design or feature problem:

1. Do **not** fix it inline.
2. Write a `proposed` ADR (type: design) with the finding and options.
3. Note it in `STATE.md` and in this packet's output.
4. The adjudication packet (OP-006/OP-007) routes it to Mikael; if accepted,
   the ADR's `Consequences` field defines the new packet(s).

## Handover

On completion, append a `STATE.md` entry: section count, any ADR escalated,
any parking-lot items logged, how many sections were flagged
inferred/unconfirmed, and that OP-005b (Knowledge Base Foundation intent) is
next.
