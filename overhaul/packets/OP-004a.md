# OP-004a: Map Deke.Core (domain models and interfaces)

## Status: COMPLETE (2026-07-07, via Claude Code)

## Contract

- **Goal:** Produce one `docs/PROJECT-MAP.md` entry per major component/module
  within `src/Deke.Core`, DEKE's dependency-free domain-model/interfaces
  project.
- **Context budget:**
  - ALWAYS: `docs/GLOSSARY.md`, `overhaul/STATE.md`
  - THIS PACKET: `overhaul/CHARTER.md` (escalation path);
    `OVERHAUL-SKETCH-v0.2.md` §3.1 (PROJECT-MAP.md entry template), §3.4 (ADR
    template), §4 (packet contract format); `src/Deke.Core/` (full — flat
    `Interfaces/` and `Models/` folders, ~38 files, no dependencies per
    `Deke.Core.csproj`); `docs/adr/ADR-0001-....md` (tone/format reference for
    any new ADR); `overhaul/PARKING-LOT.md` (existing entry format).
  - Known ties to prior packets: GLOSSARY.md's `IChunker`/`SemanticChunkerAdapter`
    row (namingissues.md #5) — the `IChunker` interface lives here;
    `ITrustScoringService`'s signature is relevant background for ADR-0005
    (federation ranking formula drift, already escalated in OP-003) but is
    not itself re-escalated by this packet.
- **Tool budget:** Read/Grep/Glob (required). Serena `get_symbols_overview`/
  `find_symbol` optional, for token-efficient reading. No Edit/Write against
  `src/` — this packet is read-only against source code.
- **Recommended tier:** Sonnet-class, default thinking (mapping sweep, per
  `docs/TOOLING.md` model/thinking tiers table).
- **Inputs:** OP-001 (workspace exists), OP-002 (`docs/TOOLING.md` tool
  verdicts), OP-003 (`docs/GLOSSARY.md` rows to cross-check).
- **Outputs:** `docs/PROJECT-MAP.md` (new file, created by this packet);
  `docs/adr/ADR-0006` (new, proposed) only if a genuine design smell surfaces;
  `overhaul/PARKING-LOT.md` (appended if minor issues surface);
  `overhaul/STATE.md` (new top entry); this packet file's Status header.
- **Done when:** every major component/module in `src/Deke.Core` has exactly
  one `docs/PROJECT-MAP.md` entry per the §3.1 template, each with an honest
  `Confidence` rating and an actual `Sources read` list (no fabricated HIGH
  confidence); any genuine design/behavior smell is escalated as a proposed
  ADR rather than fixed inline; any minor cross-doc gap is logged to
  `PARKING-LOT.md`; `STATE.md` has a new top entry naming OP-004b as next.
- **Human touchpoints:** only if a new ADR is escalated — "approve, reject, or
  amend ADR-000n — confirm?" Otherwise none; additive mapping is not itself a
  decision Mikael must make until the OP-006 interview packet.
- **Out of scope:** fixing any smell inline; touching `specification.md`,
  `retrieval-pipeline.md`, `federation.md`, `README.md`, top-level `CLAUDE.md`,
  `decisions.md`, or `evolution-vision.md`; resolving the already-pending
  ADR-0002..0005 or the 3 PROPOSED glossary rows (additive only); any other
  repo region (`Deke.Infrastructure`, `Deke.Api`, `Deke.Mcp`, `Deke.Worker`,
  `tests/`) — each is its own OP-004 sub-packet.

## Escalation path (standard, applies to every packet)

If this packet uncovers a design or feature problem:

1. Do **not** fix it inline.
2. Write a `proposed` ADR (type: design) with the finding and options.
3. Note it in `STATE.md` and in this packet's output.
4. The adjudication packet (OP-006/OP-007) routes it to Mikael; if accepted,
   the ADR's `Consequences` field defines the new packet(s).

## Handover

On completion, append a `STATE.md` entry: entry count, any ADR escalated,
any parking-lot items logged, confidence levels assigned, and that OP-004b
(Deke.Infrastructure sweep) is next.
