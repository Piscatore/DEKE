# OP-009a: Normalize P1-N shorthand and renumber Phase 5 to Phase 4

## Status: done (2026-07-14)

## Contract

- **Goal:** Normalize Package 1's phase shorthand to canonical `P1-N` across
  `specification.md` and `decisions.md`, and renumber Package 1's Phase 5
  (multilingual model swap) to Phase 4 in `retrieval-pipeline.md` and
  anywhere else it is referenced, per ADR-0003's resolution.
- **Context budget:**
  - ALWAYS: `docs/GLOSSARY.md`, `overhaul/STATE.md`
  - THIS PACKET: `docs/adr/ADR-0003-package-1-phase-shorthand-and-phase-4-gap.md`
    (full, including Resolution — the decision this packet implements);
    `docs/architecture/specification.md` (full — contains `P1-Phase1`-style
    shorthand, e.g. line 378, to be normalized to `P1-1`); `docs/architecture/decisions.md`
    (full — contains mixed shorthand: "Package 1 Phase 1" at line 27,
    "P1-Phase 2" at lines 260/276, "P1-3" at line 165 — all to be normalized
    to `P1-N`); `docs/architecture/retrieval-pipeline.md` (full — Phase 3 at
    line 144, Phase 5 at line 190 "Phase 5 of Package 1," to be renumbered:
    Phase 5 → Phase 4; verify no other phase references in this file need
    adjusting as a result).
- **Tool budget:** Read/Grep/Glob (required) against `docs/`; Edit for the
  three target files. Grep across all of `docs/` first to confirm no other
  file references "Phase 5" or inconsistent `P1-*` shorthand beyond the three
  named files — if found, include them in scope rather than leaving a
  partial normalization.
- **Recommended tier:** Sonnet-class, default thinking — this is a spec-
  refactor against an already-decided naming/numbering scheme, per
  `docs/TOOLING.md`'s model/thinking tiers table, not a fresh design
  decision.
- **Inputs:** ADR-0003 (accepted, with Resolution recorded 2026-07-07).
- **Outputs:** `docs/architecture/specification.md` (shorthand normalized to
  `P1-N`); `docs/architecture/decisions.md` (shorthand normalized to `P1-N`;
  historical entries' substance unchanged, only the phase-reference notation
  is normalized); `docs/architecture/retrieval-pipeline.md` (Phase 5 renamed
  to Phase 4 for the multilingual model swap); `overhaul/STATE.md` (new top
  entry).
- **Done when:** every Package 1 phase reference across `docs/` uses the
  canonical `P1-N` shorthand; no document references a "Phase 5" of Package
  1 — the multilingual model swap is uniformly Phase 4; Package 1's phase
  table is internally consistent (Phase 1 = provenance schema, Phase 2 =
  quality pipeline, Phase 3 = terminology database, Phase 4 = multilingual
  model swap, nothing above 4) across all three files; `STATE.md` has a new
  top entry.
- **Human touchpoints:** none expected — this packet implements an already-
  accepted ADR mechanically.
- **Out of scope:** the Three-Package Architecture doc reconciliation
  (OP-008a); implementing the Anthropic/Ollama LLM backends (OP-008b);
  federation ranking formula doc fix (OP-009b); any `src/` code changes;
  renumbering or touching Package 2's `P2-N` or Federation's `Phase 1-5`
  shorthand (both already internally consistent, out of ADR-0003's scope).

## Escalation path (standard, applies to every packet)

If this packet uncovers a design or feature problem:

1. Do **not** fix it inline.
2. Write a `proposed` ADR (type: design) with the finding and options.
3. Note it in `STATE.md` and in this packet's output.
4. The adjudication packet (OP-006/OP-007) routes it to Mikael; if accepted,
   the ADR's `Consequences` field defines the new packet(s).

## Handover

On completion, append a `STATE.md` entry: files normalized, confirmation the
Phase 5 → Phase 4 renumber is complete with no dangling references, and any
new ADR escalated.
