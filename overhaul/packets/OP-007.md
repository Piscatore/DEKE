# OP-007: Adjudication — apply OP-006's answers

## Status: done (lightweight — folded into the same session as OP-006; see Handover)

## Contract

- **Goal:** Formally apply OP-006's answers: update ADR/glossary statuses,
  confirm which answers spawn new packets, and verify no open
  adjudication remains before moving to the design/spec/lint/roadmap phase
  of the DAG (OP-008 onward).
- **Context budget:**
  - ALWAYS: `docs/GLOSSARY.md`, `overhaul/STATE.md`
  - THIS PACKET: `overhaul/packets/OP-006.md` (this session's answers);
    `docs/adr/ADR-0010-vectorless-rag-hybrid-research-direction.md`.
- **Tool budget:** Read/Edit (Write already done inline during OP-006, since
  each answer's natural home — an ADR or an `docs/INTENT.md` section —
  already existed and there was no separate "answers" artifact to apply
  changes *from*).
- **Recommended tier:** strongest available (per `docs/TOOLING.md`'s
  adjudication tier) — ran inline at the tier already active this session.
- **Inputs:** OP-006 (this session's 3 answers).
- **Outputs:** `overhaul/STATE.md` (new top entry); this packet file's
  Status header; confirmation that `docs/GLOSSARY.md` and `docs/adr/`
  need no further edits.
- **Done when:** every OP-006 answer has a permanent home (ADR, glossary
  row, or `docs/INTENT.md` section) and its status reflects the answer, not
  a placeholder; `STATE.md` names the next packet.
- **Human touchpoints:** none — Mikael's decisions were already captured in
  OP-006; this packet only formalizes bookkeeping.
- **Out of scope:** doing ADR-0010's actual research/design work (that's a
  future roadmap packet per OP-011); re-opening any question OP-006 already
  closed.

## Handover

Applied directly (no separate step needed — OP-006's answers were written
straight into their permanent homes as they were received, since this was a
lightweight, same-session run rather than a fresh adjudication session
working from a written answer-backlog):

- **ADR-0010** created with `Status: accepted` — no further glossary or ADR
  edit needed (it's a new decision, not a naming term).
- `docs/GLOSSARY.md` — verified unchanged; no new naming terms surfaced by
  either OP-006 answer.
- `docs/INTENT.md` — 2 sections updated in place (breaking-changes
  confirmed general; adhoqnotes.md resolved to "promote", citing ADR-0010).
  No sections remain `(inferred, unconfirmed)` as of this packet's
  completion.
- **Exit-criteria check (§8, `OVERHAUL-SKETCH-v0.2.md`):** criteria 1
  (glossary no PROPOSED rows) and 4 (ADRs — none proposed) are now both
  satisfied. Criterion 2 (PROJECT-MAP no LOW-confidence/unescalated smells)
  and 3 (INTENT no unconfirmed intentions) are also satisfied as of this
  packet. Criteria 5-8 (tooling verification re-run, spec lint, roadmap
  rebuild, fresh-agent acceptance test) remain — that's OP-010/011/012/013's
  job, not this one's.
- **Next:** OP-008a..e — the design packets already spawned by ADR-0002,
  ADR-0007, ADR-0008, and ADR-0009 (OP-008a/b(cancelled)/c/d/e — see their
  own packet files and `STATE.md`'s prior entries for status) are the next
  code-capable work; OP-009a/b (spec refactors) can run in parallel. No
  new packet is spawned by this session's OP-006/007 round beyond
  ADR-0010's future (unsized, OP-011-deferred) roadmap item.

Open questions for Mikael: none new.
