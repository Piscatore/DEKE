# OP-009b: Rewrite federation.md's Result Ranking section to the real formula

## Status: done (2026-07-14)

## Contract

- **Goal:** Rewrite `federation.md`'s "Result Ranking" section (lines
  231–248) to document the actual five-factor ranking formula implemented in
  `TrustScoringService.Score()`, per ADR-0005's resolution.
- **Context budget:**
  - ALWAYS: `docs/GLOSSARY.md`, `overhaul/STATE.md`
  - THIS PACKET: `docs/adr/ADR-0005-federation-ranking-formula-doc-vs-code-drift.md`
    (full, including Resolution — the decision this packet implements);
    `docs/architecture/federation.md` lines 231–248 (the stale "Result
    Ranking" section, currently `final_score = similarity * locality_weight`
    with a two-factor worked example using 0.80 local vs. 0.85 peer);
    `src/Deke.Infrastructure/Trust/TrustScoringService.cs` (full —
    `Score()`'s real formula: `similarity * confidence * credibility *
    recencyDecay * localityWeight`); `src/Deke.Core` federation-related
    contracts if needed to describe each factor accurately (per OP-004a's
    PROJECT-MAP.md entry for Search & Trust Contracts / Federation).
- **Tool budget:** Read/Grep (required) against `docs/architecture/federation.md`
  and `src/Deke.Infrastructure/Trust/TrustScoringService.cs`; Edit for
  `federation.md` only. Serena `find_symbol`/`get_symbols_overview`
  recommended to confirm `Score()`'s exact current implementation before
  writing the doc — do not paraphrase from ADR-0005 alone, re-verify against
  the live source in case it has drifted further since OP-003.
- **Recommended tier:** Sonnet-class, default thinking — this is a spec-
  refactor documenting already-decided-to-be-correct code, per
  `docs/TOOLING.md`'s model/thinking tiers table, not a design decision.
- **Inputs:** ADR-0005 (accepted, with Resolution recorded 2026-07-07).
- **Outputs:** `docs/architecture/federation.md` (Result Ranking section
  rewritten with the real five-factor formula — `similarity * confidence *
  credibility * recencyDecay * localityWeight` — including an updated worked
  example that reflects all five factors, replacing the stale two-factor
  one); `overhaul/STATE.md` (new top entry).
- **Done when:** `federation.md`'s Result Ranking section states the exact
  five-factor formula matching `TrustScoringService.Score()`'s current
  implementation; the worked example uses non-neutral values for all five
  factors (not just similarity/locality) so it accurately demonstrates
  real-world ranking behavior; no other section of `federation.md` is
  altered beyond what's needed for this fix; `STATE.md` has a new top entry.
- **Human touchpoints:** none expected — this packet implements an already-
  accepted ADR mechanically.
- **Out of scope:** the Three-Package Architecture doc reconciliation
  (OP-008a); implementing the Anthropic/Ollama LLM backends (OP-008b); P1-N
  shorthand normalization (OP-009a); any `src/` code changes — the scoring
  logic itself is confirmed correct and is not being modified, only
  documented.

## Escalation path (standard, applies to every packet)

If this packet uncovers a design or feature problem:

1. Do **not** fix it inline.
2. Write a `proposed` ADR (type: design) with the finding and options.
3. Note it in `STATE.md` and in this packet's output.
4. The adjudication packet (OP-006/OP-007) routes it to Mikael; if accepted,
   the ADR's `Consequences` field defines the new packet(s).

## Handover

On completion, append a `STATE.md` entry: confirmation `federation.md` now
matches `TrustScoringService.Score()`, and any new ADR escalated.
