# OP-001: Bootstrap the Overhaul workspace

## Status: COMPLETE (2026-07-03, seeded in chat with Claude, not Claude Code)

## Contract

- **Goal:** Create the `/overhaul` skeleton plus seed files, so later packets
  have somewhere to write. Do not touch the existing `/docs` folder — later
  packets add individual named files there directly, as needed.
- **Context budget:**
  - ALWAYS: `/overhaul/STATE.md`
  - THIS PACKET: `OVERHAUL-SKETCH-v0.2.md`
- **Tool budget:** filesystem / git only. No MCPs needed.
- **Recommended tier:** Sonnet-class, default thinking (mechanical).
- **Inputs:** none (first packet).
- **Outputs:** `/overhaul/{CHARTER.md, STATE.md, PARKING-LOT.md, packets/}`.
- **Done when:** skeleton exists and is committed; `STATE.md` has a seed
  entry.
- **Human touchpoints:** none required to execute; Mikael reviews the
  commit.
- **Out of scope:** any content in `GLOSSARY.md`, `PROJECT-MAP.md`, etc. —
  those come from later packets.

## Handover

See `STATE.md` — seed entry already appended. Next packet is OP-002.
