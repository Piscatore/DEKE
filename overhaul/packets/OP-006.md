# OP-006: Interview packet — closed questions from OP-003..005

## Status: done (lightweight — most of this packet's original scope was pre-empted; see Handover)

## Contract

- **Goal:** Collect every open closed-question left over from OP-003 (glossary
  ingestion), OP-004a..f (mapping sweeps), and OP-005a..d (intent
  distillation) and put them to Mikael in one round, so no design/naming
  question sits unresolved into OP-007's adjudication.
- **Context budget:**
  - ALWAYS: `docs/GLOSSARY.md`, `overhaul/STATE.md`
  - THIS PACKET: `docs/adr/` (all 9 ADRs' current `Status:` header);
    `docs/GLOSSARY.md` (all 3 terms' `Status` column); `docs/INTENT.md` (any
    section carrying an `(inferred, unconfirmed)` flag).
- **Tool budget:** Read/Grep (required); `AskUserQuestion` (required — this
  packet's entire output is Mikael's answers).
- **Recommended tier:** strongest available (per `docs/TOOLING.md`'s
  adjudication/ADR tier), extended thinking — per the packet plan's original
  sizing. In practice this session ran it inline at the tier already active,
  since the actual remaining question set turned out small (see Handover).
- **Inputs:** OP-003 (glossary PROPOSED rows — none remained by execution
  time), OP-004a..f (ADR-0002..0009 — all already `accepted` by execution
  time), OP-005a..d (`docs/INTENT.md`'s inferred-flagged sections).
- **Outputs:** Mikael's answers, recorded directly into `docs/INTENT.md`
  (resolving inferred flags) and a new ADR (`ADR-0010`, for the one answer
  that spawns future design work) — not a separate "answers" file, since
  each answer's natural home already existed.
- **Done when:** every ADR is `accepted`/`rejected` (not `proposed`), every
  glossary row is `APPROVED`/`REJECTED` (not `PROPOSED`), and every
  `(inferred, unconfirmed)` `docs/INTENT.md` section either stays flagged
  (if genuinely still open) or is updated with Mikael's resolution.
- **Human touchpoints:** the entire packet **is** the human touchpoint —
  posed as 3 closed questions via `AskUserQuestion` (see Handover).
- **Out of scope:** re-litigating any already-`accepted` ADR (ADR-0001..
  0009); re-opening any already-`APPROVED` glossary row; doing the design
  work any answer spawns (that's the packet(s) the answer's `Consequences`
  field names).

## Handover

By the time this packet actually ran (immediately after OP-005d completed,
same session), **all 9 ADRs were already `accepted` and all 3 glossary rows
already `APPROVED`** — every prior round adjudicated ad-hoc, out of DAG
order, exactly as each of ADR-0001 through ADR-0009's own `STATE.md` entries
already noted at the time. This packet's original scope (collect+pose a
backlog of open ADR/glossary questions) was therefore almost entirely
pre-empted before it started — the same pattern as OP-008b getting
cancelled when its premise dissolved.

What was genuinely still open, and posed via a single `AskUserQuestion`
round:

1. **OP-005a's inferred aside** — does "breaking changes OK" generalize
   DEKE-wide, or was it federation-specific? **Answer: general DEKE-wide
   stance.** Recorded directly in `docs/INTENT.md`'s "Breaking changes"
   section (no ADR needed — a confirmation, not a design decision).
2. **OP-005d's `adhoqnotes.md` idea** (vectorless RAG + hybrid retrieval) —
   park it, promote it, or drop it? **Answer: promote to a real packet.**
   Recorded as **ADR-0010** (accepted), which defers sizing to OP-011
   (roadmap rebuild) since the idea needs research before it's a sized
   design/build packet. `docs/INTENT.md`'s adhoqnotes.md section updated to
   reflect the resolution.
3. **Whether OP-006/OP-007 should run at all**, given (1)/(2) above were the
   only remaining open items. **Answer: run a lightweight version anyway**
   — cheap insurance against something missed. This packet file and
   `OP-007.md` are that lightweight run.

**Next:** OP-007 — formal adjudication/close-out of this packet's two
answers (already substantively done above; OP-007 exists to record the
formal "nothing else needs adjudicating" checkpoint per the DAG). Read
`packets/OP-007.md`.
