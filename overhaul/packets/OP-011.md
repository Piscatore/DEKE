# OP-011: Roadmap rebuild — all future work re-expressed as a sized packet DAG

## Status: done

## Contract

- **Goal:** Rebuild the roadmap so every planned piece of future work is a
  sized packet in a DAG — each with goal, context budget, tool budget,
  recommended tier, and dependencies — satisfying exit criterion 7
  (`OVERHAUL-SKETCH-v0.2.md` §8).
- **Context budget:**
  - ALWAYS: `docs/GLOSSARY.md`, `overhaul/STATE.md`
  - THIS PACKET: `docs/roadmap.md`; `overhaul/PARKING-LOT.md`;
    `docs/adr/ADR-0010` (vectorless-RAG sizing deferred here) +
    `ADR-0011` (revisit trigger, no packet); `docs/TOOLING.md` (tier
    table); `docs/PROJECT-MAP.md` (ground truth for the Current State
    table); `docs/architecture/decisions.md` (EE-N action items, Open
    Design Questions); `docs/architecture/retrieval-pipeline.md` (R-phase
    inventory); `docs/architecture/federation.md` (Phase 4/5);
    `docs/science/evolution-vision.md`; `docs/INDEX.md` (link updates).
- **Tool budget:** Read/Grep/Glob/Write/Edit; `AskUserQuestion` (priority
  and disposition decisions are Mikael's); `doc-maintainer` subagent (all
  `docs/` rewrites per top-level `CLAUDE.md` Documentation Governance);
  `bash scripts/glossary-lint.sh` + `dotnet build` for verification; git.
- **Recommended tier:** strongest available + extended thinking (per
  `docs/TOOLING.md` — roadmap rebuild is explicitly listed in that tier).
- **Inputs:** OP-010 done (glossary ENFORCED, lint live); ADR-0010
  (accepted — must receive a sized research-packet slot); ADR-0011
  (accepted — spawns *no* packet, only a revisit trigger to record);
  `PARKING-LOT.md`'s ~12 unresolved items (this is the "review
  periodically" checkpoint its own header calls for).
- **Outputs:** rebuilt roadmap doc (filename per interview round 1);
  `overhaul/PARKING-LOT.md` items annotated promoted/declined;
  `overhaul/STATE.md` entry; this packet file's Handover.
- **Done when:** roadmap expresses all planned future work as packets in a
  DAG, each with context budget, tool budget, recommended tier; its Current
  State section contradicts neither `docs/PROJECT-MAP.md` nor the code
  facts below; every PARKING-LOT item has a disposition (promoted into a
  packet, or explicitly declined/left parked); glossary lint exits 0;
  `dotnet build` unaffected.
- **Human touchpoints (front-loaded):**
  - Round 1 (procedural): commit pending OP-008/009/010 work first?
    roadmap filename (`ROADMAP.md` per sketch §2 vs. keep lowercase
    `roadmap.md`)? sizing model (fully size near-term only, far horizon
    enters via sized research/sizing packets — vs. attempt to size
    everything now)? which PARKING-LOT groups get promoted to packets?
  - Round 2 (substantive): priority ordering of the candidate packet
    tracks.
- **Out of scope:** executing any roadmap packet; any `src/` change; any
  redesign (ADR path unchanged); re-litigating ADR-0010/0011.

## Pre-build fact check (2026-07-14, this session)

`docs/roadmap.md` is stale beyond its known PARKING-LOT items — all three
"Not yet built" claims are false:

1. **Simplified trust layer (MVP item 1, "Planned")** — built: `init.sql`
   has `credibility` (sources, :26), `confidence` (facts, :43),
   `valid_from`/`valid_until` (:52-53); `TrustScoringService` implements
   the 5-factor formula (ADR-0005, verified live in OP-009b).
2. **Semantic chunking (MVP item 2, "Planned")** — built: `IChunker`/
   `SemanticChunkerAdapter` wired and used by two Worker services
   (GLOSSARY row ENFORCED; OP-004e confirmed by direct read).
3. **Bootstrap ingestion (MVP item 7, "Planned")** — built:
   `BootstrapIngestionService` + `--bootstrap` CLI branch (known
   PARKING-LOT item, OP-004e).

Also stale: "Local LLM backend — Ollama integration" listed under Future —
already delivered (advisory `IChatClient` Ollama backend, ADR-0006).
Consequence: **the MVP is fully delivered**; the rebuilt roadmap starts
from a post-MVP baseline.

## Handover

**Round 1 (procedural, `AskUserQuestion`):**
1. Pending OP-008/009/010 work: **one batch commit** before OP-011 started
   (`aed86bb`, same bundling pattern as `b52c30a`) — OP-011.md itself
   excluded, committed with this packet instead.
2. Filename: **rename `docs/roadmap.md` → `docs/ROADMAP.md`** (sketch §2,
   consistent with sibling overhaul artifacts). Bonus: three inbound links
   (`federation.md`, `retrieval-pipeline.md`, `specification.md`) pointed at
   a nonexistent `product/roadmap.md` path — already broken before this
   packet, fixed by the rename pass.
3. Sizing model: **tiered** — near-term packets fully sized; far-horizon
   work enters the DAG via sized research/sizing packets or explicit
   triggers (the ADR-0010/ADR-0011 pattern generalized).
4. PARKING-LOT groups: Mikael delegated ("best advice"). Applied: promote
   **doc-sync (HYG-1)**, **missing endpoints (HYG-2)**, **test coverage
   (HYG-3)**; **decline code-cleanup nits** (DomainStats location, shared
   ingestion helper, `GetDomainAdvice` rename) — parked as a ride-along row
   in ROADMAP.md's Trigger-Gated table.

**Round 2 (substantive, `AskUserQuestion`):** all four recommendations
accepted:
1. **P1-1 (trust & provenance schema) is first priority** — decisions.md's
   2026-03 critical-path ruling, sharpened by bootstrap ingestion having
   already run (data is accumulating on the old schema).
2. **RES-1 (ADR-0010 vectorless-RAG research) runs early and parallel** —
   read-only, findings due before R4/R6 investment decisions.
3. **Old roadmap's "Hybrid search (R2, BM25)" folded into RES-1** — its R2
   label collided with retrieval-pipeline.md's real R2 (five-level dedup)
   and BM25 hybrid appeared nowhere in the pipeline doc; it is ADR-0010's
   territory.
4. **EE1-DESIGN gets a sized slot now** (not trigger-deferred) — ADR-0002
   package parity means a live slot; ADR-0011's revisit trigger fires
   exactly inside this packet.

**Pre-build fact check** (see section above) drove the Current State
rewrite: all three "Not yet built" claims in the old roadmap were false
(trust layer, semantic chunking, bootstrap ingestion all shipped), the
"Local LLM backend" future item was already delivered — **the MVP is
delivered in full**, so the rebuilt roadmap opens from a post-MVP baseline.

**Execution:**
- `docs/ROADMAP.md` written: 5 ready-now packets (P1-1 first, RES-1, HYG-1/2/3),
  6 dependency-gated sized packets (R2, P1-2, R4, R5, EE1-DESIGN + RES-1's
  spawned design packet), 8 later sized packets (R7, R8, P1-3, R3, FED-4,
  FED-5, DOM-2, P1-4), and a Trigger-Gated table (R6, EE-2..EE-5 +
  Curiosity via EE1-DESIGN, OI-01..OI-10 via decisions.md's Review
  Schedule, parked nits). Every sized packet carries dependencies, context
  budget, tool budget, and tier per `docs/TOOLING.md` — exit criterion 7's
  exact shape. Packet IDs reuse ENFORCED shorthand (P1-N, R2-R8, EE-N)
  plus HYG-/RES-/FED-/DOM- for unlabeled work.
- Installed via `doc-maintainer` (per Documentation Governance): old file
  deleted, 7 inbound link sites fixed across 6 files (including the 3
  pre-broken ones), `INDEX.md` entries updated, case-only rename staged
  explicitly in git (NTFS is case-insensitive; without `git rm --cached` a
  Linux CI checkout would have kept the old name and broken every fixed
  link). doc-maintainer also updated `.claude/doc-maintainer.json`'s scope
  path for the rename.
- `overhaul/PARKING-LOT.md`: all 12 open items dispositioned at this
  checkpoint (6 promoted, 1 resolved directly, 1 split
  resolved/promoted, 4 reviewed-and-parked).
- doc-maintainer flagged (accepted, deferred to HYG-1): `INDEX.md`'s tree
  lists only ADR-0001 though 11 ADRs exist — pre-existing staleness, rides
  along with HYG-1.
- **Verification:** `bash scripts/glossary-lint.sh` exit 0 with ROADMAP.md
  in scan scope (doc-maintainer's post-install run; subsequent edits touched
  only `overhaul/`, outside scan scope); repo-wide grep shows `roadmap.md`
  survives only in the two exempt historical files (INTENT.md, ADR-0003);
  no `.cs` files touched this packet — a fresh `dotnet build` re-run was
  blocked by a transient harness shell outage, to be re-run at commit time.

**Next:** OP-012 (review packet: verify exit criteria 1-8, re-run TOOLING.md
verification suite) — the last packet before OP-013 (dissolution).
