# OP-004e: Map Deke.Worker (background services)

## Status: done

## Contract

- **Goal:** Produce one `docs/PROJECT-MAP.md` entry per major component/module
  within `src/Deke.Worker`, DEKE's background-services project (source
  monitoring, learning cycles).
- **Context budget:**
  - ALWAYS: `docs/GLOSSARY.md`, `overhaul/STATE.md`
  - THIS PACKET: `overhaul/CHARTER.md` (escalation path);
    `OVERHAUL-SKETCH-v0.2.md` §3.1 (PROJECT-MAP.md entry template), §3.4 (ADR
    template), §4 (packet contract format); `docs/PROJECT-MAP.md` (existing
    entries from OP-004a..d — append, do not overwrite); `src/Deke.Worker/`
    (read via Glob/Grep/Serena, don't assume structure); `docs/adr/ADR-0001-....md`
    (tone/format reference).
  - Known ties to prior packets: namingissues.md #5 states `IChunker`/
    `SemanticChunkerAdapter` is "used by two Worker services" — this sweep
    should identify which two and confirm the citation.
- **Tool budget:** Read/Grep/Glob (required). Serena `get_symbols_overview`/
  `find_symbol` recommended. No Edit/Write against `src/` — read-only against
  source code.
- **Recommended tier:** Sonnet-class, default thinking (mapping sweep, per
  `docs/TOOLING.md` model/thinking tiers table).
- **Inputs:** OP-001..003; OP-004a..d (`docs/PROJECT-MAP.md` exists — append).
- **Outputs:** `docs/PROJECT-MAP.md` (append entries for this region);
  `docs/adr/ADR-000n` (new, proposed, next available number) only if a
  genuine design smell surfaces; `overhaul/PARKING-LOT.md` (appended if minor
  issues surface); `overhaul/STATE.md` (new top entry); this packet file's
  Status header.
- **Done when:** every major component/module in `src/Deke.Worker` has
  exactly one `docs/PROJECT-MAP.md` entry per the §3.1 template, each with an
  honest `Confidence` rating and an actual `Sources read` list; any genuine
  design/behavior smell is escalated as a proposed ADR rather than fixed
  inline; any minor cross-doc gap is logged to `PARKING-LOT.md`; `STATE.md`
  has a new top entry naming OP-004f as next.
- **Human touchpoints:** only if a new ADR is escalated — "approve, reject, or
  amend ADR-000n — confirm?" Otherwise none.
- **Out of scope:** fixing any smell inline; touching `specification.md`,
  `retrieval-pipeline.md`, `federation.md`, `README.md`, top-level `CLAUDE.md`,
  `decisions.md`, or `evolution-vision.md`; resolving the already-pending
  ADR-0001..0005 or the 3 PROPOSED glossary rows (additive only); any other
  repo region — each is its own OP-004 sub-packet.

## Escalation path (standard, applies to every packet)

If this packet uncovers a design or feature problem:

1. Do **not** fix it inline.
2. Write a `proposed` ADR (type: design) with the finding and options.
3. Note it in `STATE.md` and in this packet's output.
4. The adjudication packet (OP-006/OP-007) routes it to Mikael; if accepted,
   the ADR's `Consequences` field defines the new packet(s).

## Handover

On completion, append a `STATE.md` entry: entry count, any ADR escalated,
any parking-lot items logged, confidence levels assigned, and that OP-004f
(tests/ sweep) is next.
