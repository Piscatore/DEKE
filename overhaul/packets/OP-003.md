# OP-003: Ingest namingissues.md → GLOSSARY rows

## Status: COMPLETE (2026-07-07, via Claude Code)

## Contract

- **Goal:** Normalize `thoughts/shared/namingissues.md`'s 9 findings into
  canonical `docs/GLOSSARY.md` rows (all PROPOSED), escalating genuine
  design/behavior drift as proposed ADRs and minor cross-doc gaps to
  `PARKING-LOT.md`.
- **Context budget:**
  - ALWAYS: `/overhaul/STATE.md`
  - THIS PACKET: `/overhaul/CHARTER.md`; `OVERHAUL-SKETCH-v0.2.md` §3.2
    (GLOSSARY.md template), §3.4 (ADR template), §4 (packet contract);
    `thoughts/shared/namingissues.md` (full); `docs/adr/ADR-0001-....md`
    (tone/format reference); `docs/INDEX.md` (conventions);
    `overhaul/PARKING-LOT.md` (existing entry format)
- **Tool budget:** Read/Grep only. No code changes. Roslyn/Serena symbol
  search optional, used only to confirm code citations (e.g. `LlmProvider`
  enum, `TrustScoringService.Score()`, `IChunker` DI wiring) before writing
  them into ADRs — this packet does not modify source.
- **Recommended tier:** Sonnet-class, default thinking; escalate to extended
  thinking only when a finding is ambiguous between naming, design smell, and
  cross-doc gap (per CHARTER.md's escalation path).
- **Inputs:** OP-001 (workspace exists), OP-002 (`docs/TOOLING.md`, tool
  verdicts).
- **Outputs:** `docs/GLOSSARY.md` (new, 3 PROPOSED rows); `docs/adr/ADR-0002`
  through `ADR-0005` (new, proposed); `overhaul/PARKING-LOT.md` (4 entries
  appended); `docs/INDEX.md` (Overhaul Artifacts section added); this file.
- **Done when:** every one of the 9 `namingissues.md` findings maps to
  exactly one outcome (glossary row, proposed ADR, or parking-lot entry —
  none dropped); all glossary rows have `Status=PROPOSED` and
  `Decided in="—"`; every ADR follows the ADR-0001 shape (Status / Type /
  Decision / Why / Rejected alternatives / Consequences); PARKING-LOT.md
  entries follow the established format; `docs/INDEX.md` links to
  `GLOSSARY.md`; `STATE.md` has a new top entry; none of `specification.md`,
  `retrieval-pipeline.md`, `federation.md`, `README.md`, `CLAUDE.md`,
  `decisions.md`, or `evolution-vision.md` were modified.
- **Human touchpoints:** Mikael approves/rejects/amends ADR-0002 through
  ADR-0005; Mikael approves/rejects/amends the 3 PROPOSED glossary rows.
  Phrased as a closed list at the end of the session: "Approve, reject, or
  amend each ADR and glossary row — confirm?"
- **Out of scope:** fixing any of the underlying docs inline
  (`specification.md`, `retrieval-pipeline.md`, `federation.md`, `README.md`,
  `CLAUDE.md`, `decisions.md`, `evolution-vision.md`); implementing missing
  LLM backends; correcting the federation ranking formula; `PROJECT-MAP.md`
  (that's OP-004).

## Escalation path

If this packet uncovers a design or feature problem:

1. Do **not** fix it inline.
2. Write a `proposed` ADR (type: naming or design) with the finding and
   options.
3. Note it in `STATE.md` and in this packet's output.
4. The adjudication packet (OP-006/OP-007) routes it to Mikael; if accepted,
   the ADR's `Consequences` field defines the new packet(s).

## Handover

On completion, append a `STATE.md` entry: glossary rows added, ADRs raised
(one-line titles), parking-lot items logged, and that OP-004a..n (mapping
sweeps) is next.
