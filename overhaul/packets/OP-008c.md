# OP-008c: Retire Llm/ (Gemini/OpenAI), switch PatternDiscoveryService to Advisory's IChatClient

## Status: done (2026-07-14)

## Contract

- **Goal:** Remove `src/Deke.Infrastructure/Llm/`'s independent `ILlmService`
  system and switch `Deke.Worker/Services/PatternDiscoveryService.cs` to a
  keyed `IChatClient`, per ADR-0007's resolution.
- **Context budget:**
  - ALWAYS: `docs/GLOSSARY.md`, `overhaul/STATE.md`
  - THIS PACKET: `docs/adr/ADR-0007-gemini-openai-backend-undocumented-status.md`
    (full, including Resolution — the decision this packet implements);
    `src/Deke.Infrastructure/Llm/` (full directory — `LlmConfig.cs`,
    `GeminiLlmService.cs`, `OpenAiLlmService.cs`, `NoOpLlmService.cs`, being
    retired); `src/Deke.Infrastructure/Advisory/ChatClientRegistration.cs`,
    `LlmSelectionPolicy.cs` (full — the keyed `IChatClient` pattern to
    extend/reuse, not duplicate); `src/Deke.Worker/Services/PatternDiscoveryService.cs`
    (full — the sole consumer of `ILlmService`, to be switched);
    `src/Deke.Infrastructure/ServiceCollectionExtensions.cs` (the
    `AddDekeLlm` extension method being removed/replaced, and
    `AddDekeAdvisory`/`AddDekeWorker`-equivalent wiring PatternDiscoveryService
    needs instead).
  - Known ties to prior packets: `docs/PROJECT-MAP.md`'s "Llm — Gemini/OpenAI
    Backend" and "Advisory Pipeline Implementation" entries (OP-004b) — read
    both before touching either system; OP-004d (Deke.Mcp sweep) and OP-004e
    (Deke.Worker sweep) may still be pending when this runs — check
    `overhaul/STATE.md` for their status and re-read `PatternDiscoveryService.cs`
    directly rather than trusting a stale map entry if OP-004e hasn't landed.
- **Tool budget:** This packet needs a **code-capable agent/session** — Read/
  Edit/Write against `src/Deke.Infrastructure/Llm/`, `src/Deke.Infrastructure/Advisory/`,
  `src/Deke.Worker/`, `src/Deke.Infrastructure/ServiceCollectionExtensions.cs`,
  and any affected test project; `dotnet build`/`dotnet test`. Serena/Roslyn
  MCP (`find_references`) recommended to confirm `ILlmService`/`LlmProvider`
  have zero remaining consumers before deleting.
- **Recommended tier:** strongest available (Opus-class), extended thinking —
  design/feature-build packet per `docs/TOOLING.md`'s model/thinking tiers
  table, not a mechanical sweep.
- **Inputs:** ADR-0007 (accepted, resolution recorded 2026-07-07); OP-004b
  (Deke.Infrastructure mapping sweep, complete).
- **Outputs:** `src/Deke.Infrastructure/Llm/` deleted (or reduced to nothing,
  if some shared piece turns out to still be needed — confirm via
  `find_references` before keeping anything); `PatternDiscoveryService.cs`
  updated to resolve a keyed `IChatClient` (new or reused key — decide
  whether pattern-discovery needs its own cheap/local key distinct from
  Advisory's `anthropic`/`ollama` keys, given its different cost/latency
  profile noted in ADR-0007); `ServiceCollectionExtensions.cs`'s `AddDekeLlm`
  removed or repurposed; `Deke.Worker`'s DI wiring updated to call whatever
  registers the chosen `IChatClient` key(s); `appsettings.json`'s
  `GeminiApiKey`/`OpenAiApiKey` entries removed if nothing else reads them;
  `tests/Deke.Tests` updated for any tests covering the retired system;
  `docs/PROJECT-MAP.md`'s "Llm — Gemini/OpenAI Backend" entry marked
  retired/removed with a one-line pointer to this packet;
  `overhaul/STATE.md` (new top entry).
- **Done when:** `dotnet build` and `dotnet test` pass; `ILlmService`,
  `LlmProvider`, `GeminiLlmService`, `OpenAiLlmService`, `NoOpLlmService` have
  zero remaining references anywhere in `src/` or `tests/`;
  `PatternDiscoveryService` runs its cycle through the same keyed
  `IChatClient` infrastructure Advisory uses; `STATE.md` has a new top entry.
- **Human touchpoints:** confirm which `IChatClient` key pattern-discovery
  should use (existing `ollama`/`anthropic` key vs. a new dedicated key) if
  not obvious from cost/latency considerations already noted in ADR-0007 —
  otherwise none expected, this packet implements an already-accepted ADR.
- **Out of scope:** ADR-0008's fail-fast `ApiKey` startup check (OP-008d);
  Three-Package Architecture doc reconciliation (OP-008a); P1-N shorthand
  normalization (OP-009a); federation ranking formula doc fix (OP-009b);
  any change to Advisory's own selection policy beyond adding whatever key
  pattern-discovery needs.

## Escalation path (standard, applies to every packet)

If this packet uncovers a design or feature problem:

1. Do **not** fix it inline.
2. Write a `proposed` ADR (type: design) with the finding and options.
3. Note it in `STATE.md` and in this packet's output.
4. The adjudication packet (OP-006/OP-007) routes it to Mikael; if accepted,
   the ADR's `Consequences` field defines the new packet(s).

## Handover

On completion, append a `STATE.md` entry: what was removed, what
`PatternDiscoveryService` now uses, build/test status, and confirmation that
`docs/PROJECT-MAP.md`'s Llm entry reflects the retirement.
