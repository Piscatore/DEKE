# OP-012: Review packet — verify exit criteria 1-8, re-run tooling verification

## Status: done

## Contract

- **Goal:** Verify all 8 exit criteria (`OVERHAUL-SKETCH-v0.2.md` §8) and
  re-run `docs/TOOLING.md`'s verification suite, clearing the way for
  OP-013 (dissolution).
- **Context budget:**
  - ALWAYS: `docs/GLOSSARY.md`, `overhaul/STATE.md`
  - THIS PACKET: `OVERHAUL-SKETCH-v0.2.md` §8; `CHARTER.md`;
    `docs/TOOLING.md` (verification suite); `docs/PROJECT-MAP.md`
    (confidence fields); `docs/INTENT.md` (inferred flags); `docs/adr/*`
    (status lines); `docs/ROADMAP.md` (DAG shape + one packet for the
    acceptance test).
- **Tool budget:** Read/Grep/Glob; `bash scripts/glossary-lint.sh`;
  `dotnet build`/`dotnet test`; git/gh/podman CLI; postgres-mcp, roslyn,
  cwm-roslyn-navigator (suite steps); one fresh subagent (criterion 8);
  `AskUserQuestion`; doc-maintainer for any `docs/` edit.
- **Recommended tier:** Sonnet-class, default, escalate on findings (per
  TOOLING.md reviewer-packet row).
- **Inputs:** OP-011 done (criterion 7 satisfied at close).
- **Outputs:** this packet file (criterion-by-criterion evidence);
  `overhaul/STATE.md` entry; any drift found → escalation, not inline fix.
- **Done when:** every criterion has a verdict with evidence; the tooling
  suite has fresh results; open items (if any) are routed to Mikael or
  OP-013.
- **Human touchpoints (front-loaded):** criterion 8 test mode; disposition
  of the not-yet-executed DISCONNECT verdicts.
- **Out of scope:** dissolution itself (OP-013); any redesign; fixing
  drift inline.

## Exit-criteria verdicts (2026-07-14)

| # | Criterion | Verdict | Evidence |
|---|---|---|---|
| 1 | GLOSSARY: no PROPOSED, core terms ENFORCED | **PASS** | 4 rows, all ENFORCED; CI lint live (OP-010) |
| 2 | PROJECT-MAP: no LOW confidence, no unescalated smells | **PASS** | 38 entries, all Confidence HIGH; all smells escalated as ADR-0006..0009/0011 |
| 3 | INTENT: no unconfirmed inferred intentions | **PASS** | only the legend line + one section citing its own resolution (ADR-0010) |
| 4 | ADRs: no `proposed` remain | **PASS** | all 11 accepted (ADR-0001 accepted+resolved); spawned packets completed (OP-008/009) or on ROADMAP (RES-1←ADR-0010, EE1-DESIGN←ADR-0011); ADR-0004's OP-008b cancelled, superseded by ADR-0006 |
| 5 | TOOLING: suite passes, no unresolved verdicts | **PASS** | suite results below; remaining 4 unexecuted DISCONNECTs executed this packet (Mikael's call) |
| 6 | All specs pass glossary lint | **PASS** | `bash scripts/glossary-lint.sh` exit 0 (fresh run) |
| 7 | ROADMAP: all future work as sized packet DAG | **PASS** | every sized packet has deps/context/tools/tier; trigger-gated table has trigger + sizing path |
| 8 | Fresh agent + /docs + one packet completes it | **PASS** | real run of HYG-2 (below) |

## Tooling verification suite re-run (fresh, this session)

1. `git remote -v` → origin = github.com/Piscatore/DEKE.git — PASS
2. `gh auth status` → active account Piscatore — PASS
3. `dotnet build DEKE.sln` → 0 errors (5 pre-existing ICU4N warnings, down
   from 15 at OP-002) — PASS
4. `podman ps` → deke-postgres Up 11 days (healthy) — PASS
5. postgres-mcp `list_objects(public)` → exactly the 8 expected tables — PASS
6. roslyn `search_symbols` + cwm-roslyn-navigator `find_symbol`
   (TrustScoringService) → both resolve correctly — PASS
7. docker-mcp → server absent from session (DISCONNECT executed) —
   expected-FAIL satisfied
8. sqlcl → server absent from session (DISCONNECT executed) —
   expected-FAIL satisfied

**DISCONNECT completion:** a prior session had already executed most of
the list via `.claude/settings.json` (8 `deniedMcpServers`, 17
`skillOverrides: off`). Four stragglers remained live; Mikael chose
"execute remaining now". Fixed in `.claude/settings.json` this packet:
`permissions.deny` for **LSP** + **DesignSync** (LSP confirmed
disconnected immediately), **DesignSync** added to `deniedMcpServers`
(its sibling claude.ai connectors were already there — it had simply been
missed), and **playwright-skill**'s override re-keyed to its
plugin-qualified name `playwright-skill:playwright-skill` (the unqualified
key never matched, so the skill kept loading). `graphify` was already
correctly off; only its mention in user-global CLAUDE.md text survives,
which is inert.

## Criterion 8 — acceptance test (Mikael's call: real run)

Mode chosen via `AskUserQuestion`: **really execute a roadmap packet**,
not a dry-run. Packet: **HYG-2** (missing REST endpoints — code-only,
testable, reversible). A fresh general-purpose subagent was given only:
the repo path, a pointer to the HYG-2 section of `docs/ROADMAP.md`, the
running-Postgres note, and "don't commit / don't touch docs".

**Result: completed in full, zero orientation questions.**
`PUT /api/facts/{id}`, `DELETE /api/facts/{id}` (soft delete via existing
`MarkOutdatedAsync`), `PUT /api/sources/{id}` implemented with
`.RequireAuthorization()`; 12 new tests (80/80 total green); live-verified
against the real DB (embedding regeneration proven via `md5(embedding::text)`
change; soft delete proven via `is_outdated = t` with row still
retrievable; 401/400/404 paths exercised; SSRF guard honored; test data
cleaned up). `dotnet format --verify-no-changes` and glossary lint clean.

The agent's exit report listed three judgment calls the packet left open
(PUT merge-vs-replace semantics, DELETE reason source, test-harness
boundary with HYG-3) — all resolved sensibly by mirroring existing
patterns, none blocking, none of the "what is this project?" class. The
acceptance test is satisfied on its own terms: /docs plus one packet was
sufficient context to ship verified code.

**Ride-along finding** (out of packet scope, routed not fixed): top-level
CLAUDE.md's add-source example sends `"type":"Rss"` but `SourceType` has
no string-enum JSON converter, so the documented call 400s. Appended to
HYG-1's list in `docs/ROADMAP.md` via doc-maintainer.

## Handover

- All 8 exit criteria **PASS**. Nothing blocks **OP-013 (dissolution)**:
  archive `/overhaul`, tag the repo, close the subproject per
  `OVERHAUL-SKETCH-v0.2.md` §8's after-exit note.
- HYG-2 is **done ahead of schedule** (it was the acceptance-test vehicle)
  — OP-013/next session should mark it complete in `docs/ROADMAP.md` (via
  doc-maintainer) or fold that into the dissolution commit's doc pass.
- HYG-3 note for its executor: `Deke.Tests` now references `Deke.Api`
  (plain ProjectReference + `InternalsVisibleTo`); HYG-3 still owns the
  Deke.Mcp reference, the shared-fakes dedup, and the deeper
  WebApplicationFactory question.
