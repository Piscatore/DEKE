# OP-005b: Distill intent — Knowledge Base Foundation

## Status: pending

## Contract

- **Goal:** Append to `docs/INTENT.md` one section per major Knowledge Base
  Foundation design intention, distilled from its `thoughts/` artifacts.
- **Context budget:**
  - ALWAYS: `docs/GLOSSARY.md`, `overhaul/STATE.md`
  - THIS PACKET: `overhaul/CHARTER.md` (escalation path);
    `OVERHAUL-SKETCH-v0.2.md` §3.3 (INTENT.md template), §4 (packet contract
    format); `thoughts/shared/work/knowledge-base-foundation-brief.md`,
    `thoughts/shared/research/knowledge-base-foundation-research.md`,
    `thoughts/shared/plans/knowledge-base-foundation-plan.md`,
    `thoughts/shared/sessions/knowledge-base-foundation-session.md` (4
    files, ~407 lines total); `docs/INTENT.md` (exists after OP-005a —
    append, do not overwrite its Federation section(s)); `docs/PROJECT-MAP.md`'s
    Fact & Source Domain, Term & Pattern Domain, Repositories, Data Access &
    Type Handlers, Embeddings, Harvesters, Extraction entries (cross-check
    only).
  - Known ties to prior packets: none identified yet — flag any that surface.
- **Tool budget:** Read/Grep/Glob (required). No Edit/Write outside
  `docs/INTENT.md` and this packet's own Status header.
- **Recommended tier:** Sonnet-class, default thinking (per
  `docs/TOOLING.md` model/thinking tiers table's mapping-sweep tier).
- **Inputs:** OP-001..004f (`docs/PROJECT-MAP.md` cross-check); OP-005a
  (`docs/INTENT.md` exists, append-only).
- **Outputs:** `docs/INTENT.md` (appended); `overhaul/PARKING-LOT.md`
  (appended if plan/built drift or other minor issues surface);
  `overhaul/STATE.md` (new top entry); this packet file's Status header.
- **Done when:** `docs/INTENT.md` has at least one new section per major
  Knowledge Base Foundation design intention, each ≤ half a page, each with
  `What Mikael wants` / `Why (origin/context)` / `Constraints it imposes` /
  `Source`; anything synthesized rather than directly stated is flagged
  `(inferred, unconfirmed)`; `STATE.md` has a new top entry naming OP-005c
  as next.
- **Human touchpoints:** none directly — flagged intentions surface for
  confirmation at OP-006, not resolved here.
- **Out of scope:** Federation, Advisory Pipeline MVP, or the
  docs-overhaul/product-checkpoint-fixes/llm-provider-config/adhoq-notes
  threads (each its own OP-005 sub-packet); fixing any plan-vs-built drift
  inline; editing `docs/PROJECT-MAP.md` (read-only cross-check only);
  overwriting or restructuring OP-005a's `docs/INTENT.md` sections.

## Escalation path (standard, applies to every packet)

If this packet uncovers a design or feature problem:

1. Do **not** fix it inline.
2. Write a `proposed` ADR (type: design) with the finding and options.
3. Note it in `STATE.md` and in this packet's output.
4. The adjudication packet (OP-006/OP-007) routes it to Mikael; if accepted,
   the ADR's `Consequences` field defines the new packet(s).

## Handover

On completion, append a `STATE.md` entry: section count, any ADR escalated,
any parking-lot items logged, how many sections were flagged
inferred/unconfirmed, and that OP-005c (Advisory Pipeline MVP intent) is
next.
