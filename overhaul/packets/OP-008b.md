# OP-008b: Implement the Anthropic and Ollama LLM backends

## Status: CANCELLED

> **2026-07-07, Mikael, direct adjudication (via ADR-0006, ad-hoc, ahead of
> OP-006/OP-007):** Cancelled. This packet's premise (built on ADR-0004's
> claim that no Anthropic/Ollama implementation exists) was factually wrong.
> Both backends already exist and are fully wired — see
> `src/Deke.Infrastructure/Advisory/ChatClientRegistration.cs` +
> `LlmSelectionPolicy.cs`, and the "Advisory Pipeline Implementation" /
> "Llm — Gemini/OpenAI Backend" entries in `docs/PROJECT-MAP.md`. Nothing in
> this packet's contract needs doing. The `Llm/` (Gemini/OpenAI) system's own
> undocumented status is tracked separately as ADR-0007, not through this
> packet.

## Status (original, superseded): pending

## Contract

- **Goal:** Implement the missing Anthropic and Ollama `IChatClient` backends
  in `src/Deke.Infrastructure/Llm/` so the code matches the documented LLM
  selection policy, per ADR-0004's resolution.
- **Context budget:**
  - ALWAYS: `docs/GLOSSARY.md`, `overhaul/STATE.md`
  - THIS PACKET: `docs/adr/ADR-0004-llm-backend-docs-vs-implementation-drift.md`
    (full, including Resolution — the decision this packet implements);
    `src/Deke.Infrastructure/Llm/LlmConfig.cs` (full — existing `LlmProvider`
    enum shape, currently `{ None, Gemini, OpenAi }`, to be extended);
    `src/Deke.Infrastructure/Llm/` (full directory — existing Gemini/OpenAI/
    NoOp `IChatClient` implementations, as the pattern new backends must
    follow); `docs/architecture/specification.md` lines 568–576 (LLM
    Selection Policy table: `claude-haiku-4-5` default, `claude-sonnet-5` for
    low-confidence/high-stakes, Ollama for domains allowing local models, all
    behind a single keyed `IChatClient` named `anthropic`); `docs/architecture/specification.md`
    lines 24–25 (tech stack table entries for Anthropic/Ollama via
    `IChatClient`).
- **Tool budget:** This packet needs a **code-capable agent/session** — Read/
  Edit/Write against `src/Deke.Infrastructure/Llm/` and its test project;
  `dotnet build`/`dotnet test`; NuGet MCP (or manual `dotnet add package`) to
  add an Anthropic SDK and/or Ollama client package as a dependency. This is
  **not** a docs-only packet — do not run it with a documentation-only tool
  budget. Serena/Roslyn MCP recommended for navigating existing `IChatClient`
  registrations and DI wiring.
- **Recommended tier:** strongest available (Opus-class), extended thinking —
  this is a design/feature-build packet per `docs/TOOLING.md`'s model/
  thinking tiers table, not a mechanical mapping or spec-refactor sweep.
- **Inputs:** ADR-0004 (accepted, with Resolution recorded 2026-07-07);
  OP-004b (`Deke.Infrastructure` mapping sweep — read its
  `docs/PROJECT-MAP.md` entries for the LLM component once available, for
  DI-wiring context; not a hard blocker if OP-004b hasn't landed yet).
- **Outputs:** new `AnthropicLlmService` and `OllamaLlmService` (or
  equivalently named) `IChatClient` implementations under
  `src/Deke.Infrastructure/Llm/`; `LlmConfig.cs`'s `LlmProvider` enum extended
  with `Anthropic` and `Ollama` members; DI registration wiring (likely
  `ServiceCollectionExtensions.cs`) updated to select among all backends per
  the selection policy; test coverage for the new backends in
  `tests/Deke.Tests`; `overhaul/STATE.md` (new top entry).
- **Done when:** `dotnet build` and `dotnet test` pass; both new backends
  implement the project's `IChatClient` abstraction consistently with the
  existing Gemini/OpenAI/NoOp implementations; the selection policy
  (`claude-haiku-4-5` default, `claude-sonnet-5` escalation, Ollama for
  local-model domains) is wired, even if full policy logic is a follow-on
  packet — at minimum both backends are implemented, registered, and
  selectable via configuration; `STATE.md` has a new top entry.
- **Human touchpoints:** confirm before adding new NuGet dependencies (which
  Anthropic/Ollama client packages to use) if not already obvious from
  `specification.md`; otherwise none expected — this packet implements an
  already-accepted ADR.
- **Out of scope:** the Three-Package Architecture doc reconciliation
  (OP-008a); P1-N shorthand normalization (OP-009a); federation ranking
  formula doc fix (OP-009b); building out the full selection-policy routing
  logic beyond making both backends implementable and selectable, unless
  trivial to include.

## Escalation path (standard, applies to every packet)

If this packet uncovers a design or feature problem:

1. Do **not** fix it inline.
2. Write a `proposed` ADR (type: design) with the finding and options.
3. Note it in `STATE.md` and in this packet's output.
4. The adjudication packet (OP-006/OP-007) routes it to Mikael; if accepted,
   the ADR's `Consequences` field defines the new packet(s).

## Handover

On completion, append a `STATE.md` entry: which backends were implemented,
build/test status, any new ADR escalated, and confirmation that
`LlmProvider` now matches `specification.md`'s documented policy.
