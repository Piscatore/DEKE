# OP-008a: Adopt the Three-Package Architecture

## Status: done (2026-07-14)

## Contract

- **Goal:** Reconcile `docs/product/overview.md`, `docs/science/evolution-vision.md`,
  and `docs/architecture/decisions.md` with ADR-0002's resolution — the
  Evolution Engine is an active package, and DEKE's product model is a
  Three-Package Architecture, not a Two-Package Architecture.
- **Context budget:**
  - ALWAYS: `docs/GLOSSARY.md`, `overhaul/STATE.md`
  - THIS PACKET: `docs/adr/ADR-0002-evolution-engine-package-3-naming-status.md`
    (full, including Resolution — the decision this packet implements);
    `docs/product/overview.md` (full — currently describes a "Two-Package
    Architecture"; Package 3/Evolution Engine never mentioned; Federation
    called cross-cutting rather than a package); `docs/science/evolution-vision.md`
    (full — currently states "It is not part of DEKE's active product
    model," to be removed); `docs/architecture/decisions.md` (full — the
    2026-04 entry deferring the Evolution Engine to research status, plus
    all `P3-*` entries, need re-framing as active design targets rather than
    research history; do not touch unrelated entries).
- **Tool budget:** Read/Grep/Glob (required) against `docs/`. Edit for the
  three target files. No `src/` access needed — this is a docs-only packet.
- **Recommended tier:** strongest available (Opus-class), extended thinking —
  this is a design/spec reconciliation packet per `docs/TOOLING.md`'s
  model/thinking tiers table, not a mechanical mapping sweep.
- **Inputs:** ADR-0002 (accepted, with Resolution recorded 2026-07-07).
- **Outputs:** `docs/product/overview.md` (Evolution Engine added as Package
  3, "Two-Package Architecture" framing replaced with "Three-Package
  Architecture"); `docs/science/evolution-vision.md` (stale "not part of
  DEKE's active product model" claim removed, replaced with active-package
  framing consistent with ADR-0002); `docs/architecture/decisions.md`
  (2026-04 deferral entry and `P3-*` entries re-framed as active design
  targets, not research history — the entries themselves are historical
  record and should not be deleted, only annotated/re-framed per this
  project's decision-log conventions); `overhaul/STATE.md` (new top entry).
- **Done when:** all three files consistently describe a Three-Package
  Architecture with the Evolution Engine at parity with Package 1/Package 2;
  no remaining sentence in any of the three files states or implies the
  Evolution Engine is deferred, research-only, or not part of the active
  product model; `decisions.md`'s historical entries remain readable as a
  decision log (not deleted) but are annotated to reflect the 2026-07-07
  reversal; `STATE.md` has a new top entry.
- **Human touchpoints:** none expected — this packet implements an already-
  accepted ADR. If the agent finds the reconciliation requires a design
  choice ADR-0002 didn't settle (e.g., renumbering Federation as a package),
  escalate as a new proposed ADR rather than deciding unilaterally.
- **Out of scope:** implementing the Anthropic/Ollama LLM backends (OP-008b);
  P1-N shorthand normalization (OP-009a); federation ranking formula doc fix
  (OP-009b); any `src/` code changes.

## Escalation path (standard, applies to every packet)

If this packet uncovers a design or feature problem:

1. Do **not** fix it inline.
2. Write a `proposed` ADR (type: design) with the finding and options.
3. Note it in `STATE.md` and in this packet's output.
4. The adjudication packet (OP-006/OP-007) routes it to Mikael; if accepted,
   the ADR's `Consequences` field defines the new packet(s).

## Handover

On completion, append a `STATE.md` entry: which files were changed and how,
any new ADR escalated, and confirmation that the Three-Package Architecture
is now consistently documented across `overview.md`, `evolution-vision.md`,
and `decisions.md`.
